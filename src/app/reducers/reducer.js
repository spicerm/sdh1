const reducer = (state={
//Application State
}, action)=>{
    switch (action.type){
        case "":
            return {}
        default:
            return state;
    }
};

export default reducer;